# Story 4.1: Gatilhos de checkpoint (outbox compartilhado)

## Status

Review

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run lint", "npm run build-tsc", "npm run build"]

## Story

**Como o** sistema meDIZ,
**eu preciso** de um mecanismo que detecte, sem depender de um "fim de conversa" que não existe no produto, quando uma fatia nova de conversa está pronta para ser processada,
**para que** o ID Vida (e futuramente a atualização contínua do ID Pessoa) tenham um outbox confiável de onde consumir trechos de conversa, sem cron de inatividade e sem reprocessar o que já foi extraído.

## Acceptance Criteria

1. Novo model `ConversationEvent` em `prisma/schema.prisma` — outbox genérico e compartilhado (não exclusivo do ID Vida; desenhado para também alimentar, numa story futura, a atualização contínua do ID Pessoa — `core`/`dynamics`/`predictive`), reaproveitando a mesma base de `DiscoveryEvent` (verificado em `prisma/schema.prisma` de `origin/develop`): `id`, `userId`, `eventType` (default `'conversation.checkpoint.v1'`), `schemaVersion`, `payload` (Json), `status` (default `'pending'`), `attempts`, `lastError`, `availableAt`, `processedAt`, `createdAt`, `updatedAt` — mais **dois campos novos que `DiscoveryEvent` não tem**: `sessionId` (necessário porque, ao contrário do evento de descoberta que é por usuário, este evento é escopado por `ChatSession`) e `trigger` (`String`, CHECK constraint `'message_count' | 'new_session'`). Incluir também os mesmos índices que `DiscoveryEvent` usa para a query `FOR UPDATE SKIP LOCKED` do endpoint `claim` não fazer table scan: índice em `userId` e índice parcial em `availableAt` para `status IN ('pending', 'failed')` (ver migration `20260618120000_add_user_profiles_and_discovery_outbox` em `origin/develop` para o SQL exato desses índices). Migration aditiva manual (sem shadow database — banco compartilhado com `indevmediz`, checar `prisma migrate status` antes de aplicar, mesmo procedimento da Story 2.1/2.2).
2. `ChatSession` ganha dois campos novos: `messageCount Int @default(0)` e `lastCheckpointMessageCount Int @default(0)`, na mesma migration.
3. `saveChatMessage` (`src/lib/chatMessages.ts`) passa a rodar dentro de uma `prisma.$transaction` que: (a) cria a `ChatMessage` como hoje; (b) incrementa `ChatSession.messageCount`; (c) se `messageCount - lastCheckpointMessageCount >= CONVERSATION_CHECKPOINT_MESSAGE_THRESHOLD` (env var nova, default `14`), insere um `ConversationEvent` (`trigger: 'message_count'`) e atualiza `lastCheckpointMessageCount = messageCount` no mesmo `update` — para não dobrar o disparo na mesma janela.
4. `createChatSessionWithThread` (`src/lib/chatService.ts`) passa a, antes de criar a sessão nova, buscar a sessão mais recente do usuário com `messageCount > lastCheckpointMessageCount` (ou seja, com conteúdo não extraído) e, se existir, enfileirar um `ConversationEvent` (`trigger: 'new_session'`) para ela.
5. `POST /api/conversation-events/claim` e `POST /api/conversation-events/result`, copiando o padrão de `src/app/api/discovery/events/{claim,result}/route.ts` (SQL raw com `FOR UPDATE SKIP LOCKED`, até 5 eventos por chamada, eventos presos em `processing` por mais de 10 min voltam para `failed`, retry com backoff exponencial até 60 min). Autenticados via `validateWebhookBearer(request, 'CONVERSATION_WEBHOOK_TOKEN')` — env var própria e dedicada, reusando a função já generalizada em `src/lib/webhookAuth.ts` (não criar um 3º padrão de bearer auth).
6. `payload` do `ConversationEvent` carrega só a referência à fatia nova da conversa desde o último checkpoint (ex.: `{ threadId, fromMessageCount, toMessageCount }`), não o conteúdo das mensagens em si — quem processar o evento (Story 4.3, fora deste repo) busca o texto via API própria quando precisar. Mantém o outbox leve e evita duplicar dado sensível em duas tabelas.
7. Enquanto `saveChatMessage` está sendo alterado por esta story, remover as duas chamadas `fetch()` de debug hoje hardcoded na função (`http://127.0.0.1:7242/ingest/...`, com `contentPreview` do conteúdo da mensagem) — instrumentação de depuração esquecida em commit anterior, sem relação com este código, que hoje roda em produção a cada mensagem de chat.
8. **Fora de escopo desta story**: qualquer extração de conteúdo (fatos do ID Vida ou perfil do ID Pessoa) — só os dois gatilhos de disparo e o enfileiramento. Também fora de escopo: cron de inatividade para sessões curtas abandonadas (ver limitação aceita no plano — vira Story 4.5 só se os dados mostrarem que é necessário).

## Tasks / Subtasks

- [ ] Adicionar `ConversationEvent` (com `sessionId` e `trigger`, mais os índices equivalentes aos de `DiscoveryEvent` em `userId` e `availableAt`/`status`) e os campos `messageCount`/`lastCheckpointMessageCount` em `ChatSession` ao `prisma/schema.prisma`; gerar migration com `prisma migrate dev --create-only`, checando `prisma migrate status` antes (AC: 1, 2).
- [ ] **Sem ambiente de homologação — só existe produção**: revisar manualmente o SQL gerado pela migration (deve conter só `CREATE TABLE` e `ALTER TABLE ... ADD COLUMN ... DEFAULT ...`, nenhum `DROP`/`RENAME`/mudança de tipo em coluna existente) antes de aplicar com `prisma migrate deploy`. Aplicar a migration **antes** (ou no mesmo passo de deploy) do código que referencia `ConversationEvent`/os campos novos de `ChatSession` — se o código subir antes da migration, o Prisma vai falhar nesses requests até a migration rodar (não corrompe dado, mas gera erro visível em prod).
- [ ] Envolver `saveChatMessage` numa transação que cria a mensagem, incrementa `messageCount`, e condicionalmente enfileira o checkpoint por contagem (AC: 3).
- [ ] Remover a instrumentação de debug (`fetch` para `127.0.0.1:7242`) de `saveChatMessage` (AC: 7).
- [ ] Ajustar `createChatSessionWithThread` para enfileirar o checkpoint de nova sessão antes de criar a sessão nova (AC: 4).
- [ ] Criar `POST /api/conversation-events/claim` e `POST /api/conversation-events/result`, espelhando os endpoints de discovery (AC: 5, 6).
- [ ] Adicionar `CONVERSATION_WEBHOOK_TOKEN` e `CONVERSATION_CHECKPOINT_MESSAGE_THRESHOLD` (default `14`) como env vars documentadas.
- [ ] Quality gates (lint, build-tsc, build) + verificação manual real: mandar mensagens numa sessão até passar do limiar e confirmar o evento criado; abrir sessão nova com sessão anterior "suja" e confirmar o evento `new_session`; testar `claim`/`result` com e sem bearer correto.

## Dev Notes

- **Fonte deste plano**: `docs/plano-id-vida-implementacao.md`, seção "Story 4.1 — Gatilhos de checkpoint (outbox compartilhado)" e seção "Gap crítico descoberto" (justifica por que o gatilho é checkpoint incremental, não "fim de conversa").
- **Padrão a copiar literalmente**: `src/app/api/discovery/events/claim/route.ts` e `.../result/route.ts` (query SQL raw com `FOR UPDATE SKIP LOCKED`, contagem de tentativas, backoff) — mesmo problema resolvido de novo, não inventar uma solução nova. Confirmado presente em `origin/develop` no momento desta story.
- **`validateWebhookBearer`** (`src/lib/webhookAuth.ts`) já aceita nome de env var customizado como segundo parâmetro (generalizada na Story 2.1) — usar `CONVERSATION_WEBHOOK_TOKEN` como a env var dedicada deste outbox, sem tocar na função.
- **Threshold `X` (revisão @architect)**: comece com `14` (meio da faixa 12–15 sugerida no plano original), mas trate como parâmetro tunável por dado real — não há baseline de tamanho médio de conversa neste app ainda. Deixar configurável por env var (`CONVERSATION_CHECKPOINT_MESSAGE_THRESHOLD`) em vez de hardcoded, exatamente para permitir recalibrar sem deploy de código depois de medir em produção.
- **Contrato do `payload`/endpoint `result` (decisão pendente resolvida na revisão @architect)**: como o outbox é compartilhado entre o braço ID Vida (Story 4.3) e uma futura story de atualização contínua do ID Pessoa, o endpoint `result` desta story deve aceitar um payload de resultado que **não assuma um único consumidor** — ou seja, não travar o schema do `result` em campos exclusivos de `UserFact`/`lifeCompact`. Nesta story (4.1), o endpoint `result` só precisa fazer o básico do outbox (marcar `processed`/`failed`, gravar `lastError`, agendar retry); a escrita específica de dados de perfil/fatos fica para as stories que de fato processam o conteúdo (4.3 e a futura story do ID Pessoa). Documentar essa divisão explicitamente no código do endpoint (comentário curto basta) para quem for abrir a Story 4.3 não presumir um contrato mais amplo do que este.
- **Achado de arquitetura não relacionado, mas tratado nesta story por tocar o mesmo arquivo**: `saveChatMessage` em `src/lib/chatMessages.ts` (em `develop`) tem hoje dois `fetch()` incondicionais para `http://127.0.0.1:7242/ingest/87541063-b58b-4851-84d0-115904928ef7`, enviando `contentPreview` (100 primeiros caracteres da mensagem) a cada mensagem salva — parece instrumentação de debug de um agente de IA (`hypothesisId`, `runId`, `sessionId: 'debug-session'`) esquecida num commit anterior. Remover como parte do AC 7, já que a função inteira está sendo reescrita para virar uma transação.
- **Assunção de ordenação**: `fromMessageCount`/`toMessageCount` no payload assume que `ChatMessage` nunca é deletada fora de ordem dentro de uma sessão (não há `delete` de `ChatMessage` encontrado no código hoje) — se isso mudar no futuro, o cálculo da fatia por contagem para de ser confiável e precisa virar referência por `id`/`createdAt`.
- **Banco compartilhado**: como descoberto na Story 2.1, o Postgres do meDIZ é compartilhado com o projeto legado `indevmediz`. Checar `prisma migrate status` antes de qualquer `migrate dev`/`deploy`.
- **Sem ambiente de homologação (revisão @po)**: o projeto roda com um único banco, que é produção — não há staging pra testar a migration antes. A migration desta story é aditiva por natureza (`CREATE TABLE ConversationEvent` + `ALTER TABLE ChatSession ADD COLUMN ... DEFAULT 0`), o que no Postgres (>=11) é operação só de metadado — não reescreve tabela, não faz lock longo mesmo em `ChatSession` sendo tabela ativa. Mesmo assim: gerar a migration com `--create-only`, ler o SQL manualmente confirmando que não há `DROP`/`RENAME`/mudança de tipo, e aplicar a migration antes (ou junto) do deploy do código que a usa — nunca depois.
- **Branch**: esta story deve ser implementada a partir de `feat/id-vida-checkpoint-outbox` (criada de `origin/develop` em 2026-07-18) — `main` não tem nenhuma das peças do ID Pessoa/Discovery das quais esta story depende (`UserProfile`, `DiscoveryEvent`, `validateWebhookBearer` generalizada). Confirmar que a branch de trabalho não é baseada em `main` antes de começar.

### Testing

- Enviar mensagens numa sessão até ultrapassar `CONVERSATION_CHECKPOINT_MESSAGE_THRESHOLD` e confirmar que exatamente um `ConversationEvent` (`trigger: 'message_count'`) é criado, com `lastCheckpointMessageCount` atualizado.
- Confirmar que mensagens adicionais dentro da mesma janela não disparam um segundo evento.
- Criar uma sessão nova para um usuário com uma sessão anterior "suja" (`messageCount > lastCheckpointMessageCount`) e confirmar o `ConversationEvent` (`trigger: 'new_session'`) para a sessão antiga.
- Criar uma sessão nova para um usuário sem sessão suja e confirmar que nenhum evento é criado.
- Testar `/api/conversation-events/claim` e `/result` com e sem bearer correto (`CONVERSATION_WEBHOOK_TOKEN`).
- Confirmar que os dois `fetch()` de debug não existem mais em `saveChatMessage` (`grep -rn "127.0.0.1:7242" src` vazio).
- Executar lint, TypeScript (`build-tsc`) e build.

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-07-18 | 1.0 | Story criada a partir de `docs/plano-id-vida-implementacao.md`, com ajustes da revisão de @architect (Aria) e @po (Pax) | River (@sm) |
| 2026-07-20 | 1.1 | Validated GO (8/10) — Status: Draft → Ready. AC1/Task1 corrigidos: `sessionId` não existe em `DiscoveryEvent` (é campo novo, não copiado) e índices de `ConversationEvent` (userId; availableAt/status parcial) tornados explícitos | @po (Pax) |
| 2026-07-20 | 1.2 | Adicionada salvaguarda explícita de deploy de migration sem ambiente de homologação (revisar SQL manualmente, aplicar antes do código) — Task/Dev Notes, a pedido do usuário | @po (Pax) |
| 2026-07-20 | 2.0 | Implementação completa (AC 1-7): schema+migration aplicada em prod, `saveChatMessage`/`createChatSessionWithThread` reescritos, endpoints `claim`/`result` criados, quality gates verdes. Status: Ready → Review. Pendente: verificação manual end-to-end (sem ambiente de homologação) | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via Claude Code — persona @dev (Dex)

### Debug Log References

N/A — sem falhas de execução a registrar. Único desvio de plano: `prisma migrate dev --create-only` falhou (P3006/P1014, shadow database quebrado por uma migration antiga não relacionada); migration gerada por `prisma migrate diff --from-url <DIRECT_URL> --to-schema-datamodel prisma/schema.prisma --script` e depois **reescrita manualmente**, pois o diff trouxe drift preexistente do banco (drops de tabelas legadas `AudioMessage`/`AudioSession`/`CreditTransaction`/`UserCredits`/`user_discovery_profiles`, `DROP TYPE`, mudanças de tipo em colunas de `discovery_events`/`user_profiles`/`product_entitlements`) sem relação com esta story. Aplicada só a migration aditiva revisada manualmente (`prisma migrate deploy`).

### Completion Notes List

- AC1, AC2: `ConversationEvent` e os campos `messageCount`/`lastCheckpointMessageCount` em `ChatSession` adicionados ao schema, com os índices equivalentes aos de `DiscoveryEvent` (userId; availableAt+status). Migration `20260720120000_add_conversation_events_outbox` aplicada em produção via `prisma migrate deploy` (sem shadow database — ver Debug Log acima).
- AC3, AC7: `saveChatMessage` reescrita como `prisma.$transaction`: cria a mensagem, incrementa `messageCount`, e se `messageCount - lastCheckpointMessageCount >= CONVERSATION_CHECKPOINT_MESSAGE_THRESHOLD` (default 14), cria o `ConversationEvent` (`trigger: 'message_count'`) e atualiza `lastCheckpointMessageCount` na mesma transação. As duas chamadas `fetch()` de debug dentro de `saveChatMessage` foram removidas. **Fora de escopo, não alterado**: `getThreadMessages` (mesmo arquivo) também tem instrumentação de debug idêntica (3 chamadas `fetch` para o mesmo endpoint `127.0.0.1:7242`) — não fazia parte do AC7, que menciona só `saveChatMessage`; deixado como está para não exceder o escopo da story. Sinalizando aqui para o @po decidir se vira um item de backlog.
- AC4: `createChatSessionWithThread` (`chatService.ts`) passa a chamar `enqueueNewSessionCheckpointIfDirty(userId)` antes de criar a sessão nova. Como Prisma não suporta comparação coluna-a-coluna (`messageCount > lastCheckpointMessageCount`) no filtro declarativo, a busca da sessão "suja" mais recente usa `$queryRaw`. **Decisão de implementação não coberta explicitamente pela AC4**: ao enfileirar o evento `new_session`, também atualizo `lastCheckpointMessageCount = messageCount` na sessão antiga (mesma transação), por simetria com o gatilho por contagem — sem isso, a mesma sessão "suja" seria reenfileirada a cada nova sessão aberta pelo usuário dali em diante, gerando eventos duplicados indefinidamente no outbox.
- AC5, AC6: `POST /api/conversation-events/claim` e `.../result` criados espelhando literalmente o padrão de `src/app/api/discovery/events/{claim,result}/route.ts` (mesma query `FOR UPDATE SKIP LOCKED`, mesmo mecanismo de reclaim de `processing` travado, mesmo backoff exponencial capado em 60min). Autenticados via `validateWebhookBearer(request, 'CONVERSATION_WEBHOOK_TOKEN')`. O endpoint `result` só implementa a mecânica genérica do outbox (`success`/`error` → `processed`/`failed` + retry), sem schema de dados extraídos — conforme decisão registrada nas Dev Notes (outbox compartilhado com futura story do ID Pessoa).
- `CONVERSATION_WEBHOOK_TOKEN` (token aleatório gerado, 32 bytes hex) e `CONVERSATION_CHECKPOINT_MESSAGE_THRESHOLD=14` adicionados ao `.env` local (que aponta pro Postgres/Accelerate de produção) e documentados em `.env.example` — **porém `.env.example` está no `.gitignore` deste repositório (`.env*` sem exceção), então essa edição não é versionada nem chega a outros devs via git**; a documentação que de fato persiste é esta story. Atenção: se o app implantado no EasyPanel não lê o mesmo `.env` usado localmente, `CONVERSATION_WEBHOOK_TOKEN` precisa ser configurado manualmente lá também como runtime env var (não build arg — mesmo cuidado já identificado no incidente do worker de PDF), senão os endpoints novos respondem 503 em produção.
- Quality gates: `npm run lint` (sem erros novos — os 3 erros existentes são pré-existentes em `LibraryPdfViewer.tsx`, não relacionados), `npm run build-tsc` (limpo), `npm run build` (sucesso, `/api/conversation-events/claim` e `/result` presentes no output).
- **Não executado nesta sessão**: verificação manual end-to-end (task da seção Testing — enviar mensagens até o limiar, criar sessão nova "suja", testar claim/result com bearer). Não há ambiente de homologação; gerar tráfego de teste real (mensagens de chat, sessões) direto em produção não foi feito para não gerar dados de teste misturados a dados reais de usuário sem alinhamento prévio. Recomendo ao @po/usuário decidir entre: (a) testar manualmente pela própria aplicação com uma conta de teste, ou (b) autorizar um script descartável específico para isso.

### File List

- `docs/stories/4.1.id-vida-checkpoint-outbox.md`
- `prisma/schema.prisma` (models `ConversationEvent`, `User.conversationEvents`, `ChatSession.messageCount`/`lastCheckpointMessageCount`/`conversationEvents`)
- `prisma/migrations/20260720120000_add_conversation_events_outbox/migration.sql` (novo, aplicado em produção)
- `src/lib/chatMessages.ts` (`saveChatMessage` reescrita)
- `src/lib/chatService.ts` (`enqueueNewSessionCheckpointIfDirty` + `createChatSessionWithThread`)
- `src/app/api/conversation-events/claim/route.ts` (novo)
- `src/app/api/conversation-events/result/route.ts` (novo)
- `.env` (local, não versionado — `CONVERSATION_WEBHOOK_TOKEN`, `CONVERSATION_CHECKPOINT_MESSAGE_THRESHOLD`)
- `.env.example` (não versionado neste repo — ver nota acima)

## QA Results

_A preencher após a implementação._
